# 💻 HTML Coding Challenges: Practice Prompts

This section tests your ability to write raw HTML5 code from scratch without looking at a tutorial. Read the prompt, visually imagine the output, and write the complete HTML structure in the space provided. Check your exact code against `Challenge_Answers.md`.

## 🛠️ Challenge 1: The Complex Data Table
**Prompt:** Build a data table displaying exam results. The table must:
1. Have a `<caption>` reading "Semester 1 Results".
2. Have a header row (`<thead>`) containing columns: "Subject", "Midterms", "Finals".
3. Use a `<tbody>` with three subjects (Math, Science, History).
4. Use the `rowspan` attribute in the "Subject" column for "Science" because it has both "Lab" and "Theory" sub-rows.
5. Have a `<tfoot>` summarizing "Status: PASS".

[ Write your HTML code here... ]

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>


## 🛠️ Challenge 2: Embedded Multi-Media Article
**Prompt:** Write a full semantic `<article>`. Inside it:
1. Include an `<h1>` heading.
2. Include a native `<audio>` player that has playback `controls` but does NOT autoplay.
3. Embed an image with the `loading="lazy"` attribute to optimize rendering.
4. Wrap a quote from the article in a `<blockquote>` and cite the author using a `<cite>` tag inside a `<figcaption>`.

[ Write your HTML code here... ]

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>


## 🛠️ Challenge 3: Advanced Form Layout
**Prompt:** Build a contact form that:
1. Uses a `<fieldset>` and `<legend>` for "User Details".
2. Has a dropdown menu (`<select>`) for "Country" with three `<option>` choices.
3. Has a set of Radio Buttons for "Gender" grouped properly by the `name` attribute so only one can be checked.
4. Includes a Checkbox that says "I agree to Terms", making the entire label clickable to check the box.
5. Has an `<input type="date">` that restricts users from picking a date before "2026-01-01".

[ Write your HTML code here... ]

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>


## 🛠️ Challenge 4: The Description List (Dictionary)
**Prompt:** Use the `<dl>`, `<dt>`, and `<dd>` tags to create a semantic dictionary of 3 common HTML terms (e.g., HTML, DOM, CSS). This is an extremely common technical test to see if you know lists beyond `<ul>` or `<ol>`.

[ Write your HTML code here... ]

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>

## 🛠️ Challenge 5: Accessible Login Form
**Prompt:** Build a login form that:
1. Uses `<label>` correctly for email and password.
2. Includes `required` on both fields.
3. Adds helper text and links it using `aria-describedby`.
4. Includes a "Remember me" checkbox with clickable label text.
5. Adds a submit button with clear text.

[ Write your HTML code here... ]

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>

## 🛠️ Challenge 6: SEO-Friendly Blog Header
**Prompt:** Create a basic HTML page `<head>` that includes:
1. A meaningful `<title>`.
2. Meta description.
3. Viewport tag.
4. Canonical link.
5. Open Graph title + description.

[ Write your HTML code here... ]

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>

## 🛠️ Challenge 7: Figure + Caption Gallery Item
**Prompt:** Create one semantic gallery item using:
1. `<figure>` wrapper.
2. Responsive image with `loading="lazy"`.
3. Descriptive `alt` text.
4. `<figcaption>` with short context text.

[ Write your HTML code here... ]

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>
